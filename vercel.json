{
  "version": 2,
  "buildCommand": "rm -rf .next && npx prisma generate && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm ci && npm install --save-dev typescript@5.8.2 eslint@9.23.0",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": false,
      "develop": false,
      "staging": false
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}
